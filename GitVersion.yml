next-version: 1.0.2
mode: ContinuousDeployment
strategies:
  - Mainline
branches:
  master:
    regex: ^master$|^main$
    is-release-branch: true
    is-main-branch: true
    increment: Patch
commit-message-incrementing: Enabled
ignore:
  sha: []